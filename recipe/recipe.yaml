schema_version: 1

context:
  version: 54.15.0

package:
  name: aws-cdk.cloud-assembly-schema
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/a/aws-cdk.cloud-assembly-schema/aws_cdk_cloud_assembly_schema-${{ version }}.tar.gz
  sha256: 3136dc4c6222cdecab0a2fd56983277fc93c19a3f7d7b7c990aabe6e8eddcbce

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=75.3.2
    - python-build >=1.5.0,<1.6.dev0
    - pip
  run:
    - python >=${{ python_min }}
    - python-jsii >=1.139.0,<2.0.0
    - publication >=0.0.3
tests:
  - python:
      imports:
        - aws_cdk.cloud_assembly_schema
        - aws_cdk.cloud_assembly_schema._jsii
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - '*'

about:
  summary: Schema for the protocol between CDK framework and CDK CLI
  license: Apache-2.0 AND MIT
  license_file:
    - NOTICE
    - LICENSE
  homepage: https://github.com/aws/aws-cdk
  repository: https://github.com/aws/aws-cdk-cli

extra:
  recipe-maintainers:
    - ocefpaf
